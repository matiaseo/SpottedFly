import React from "react";

// This component will only be used if we go for the clone of the web page version, the desktop app does not have any footer.
export const Footer = () => {
  return <div>Footer</div>;
};
